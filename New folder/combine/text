<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="styles/styles.css" />
    <title>Skollar Aid</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="chat-container">
      <header id="header">
        <h1>Skollar Aid</h1>
      </header>
      <main>
        <!-- Layout for both user message and bot message -->
        <div id="chat-history"></div>

        <!-- Form to accept user input -->
        <form id="chat-form">
          <div class="formH">
            <div class="content">
              <div class="text">
                <input
                  id="textInput"
                  type="text"
                  placeholder="Your message here"
                />
              </div>

              <!-- Uploads container which will be hidden and will only be visible when the upload icon is clicked-->
              <div id="uploads" class="uploads" style="display: none">
                <div class="image-wrapper">
                  <img id="imagePreview" alt="Image Preview" />
                  <span class="close-icon">&times;</span>
                </div>
                <input
                  id="descriptionInput"
                  type="text"
                  placeholder="Type a description"
                />
              </div>

              <!-- Upload button and file input -->
              <div class="imgg">
                <label for="imageInput">Upload</label>
                <input
                  id="imageInput"
                  accept="image/*"
                  type="file"
                  name="image"
                  hidden
                />
              </div>
            </div>
            <div class="fomb">
              <button id="sendButton" type="submit">Send</button>
            </div>
          </div>
        </form>
      </main>
    </div>

    <!-- Modal for Enlarging Image -->
    <div id="imageModal" class="modal">
      <!-- <div class="modal-content-container"> -->
      <span class="close">&times;</span>
      <img id="modalImage" class="modal-content" />
      <div id="caption"></div>
      <!-- </div> -->
    </div>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

    <script src="script/script.js"></script>
  </body>
</html>

* {
    font-family: "Poppins", sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    min-width: 100vh;
    background-color: #f5f5f5;
}

.formH {
    display: flex;
    align-items: center;
   //justify-content: space-between;
    width: 100%;
     background: #ccc;
     height: 99px;
     border-radius: 25px;
}

.content {
    display: flex;
    align-items: center;
    width: 90%;
    height: 50px;
    gap: 10px;
     
    
}

.text {
    flex: 7;
}

#textInput {
    width: 100%;
    padding: 10px;
   border: none;
    outline: none;
  background-color: #ccc;
}

#uploads {
    display: flex;
    //argin: 2px;
    border-radius: 8px;
    //background: #ff0000;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    //padding: 10px;
    height: 90px;
    width: 100%; /* Make width 100% */
    max-width: calc(100% - 20px); /* Ensure it does not overflow */
}

.imgg {
    flex: 1.5;
    display: flex;
    align-items: center;
    justify-content: center;
    position: static;
}


.image-wrapper {
    position: relative;
    display: inline-block;
}

#imagePreview {
    height: 50px;
    max-width: 100px;
    border-radius: 8px;
    display: block;
}

.close-icon {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    display: none;
    font-size: 50px;
    color: white;
    cursor: pointer;
    z-index: 10;
   
    width: 20px;
    border-radius: 50%;
    align-items: center;
}

.image-wrapper:hover .close-icon {
    display: block;
}

textarea{
    outline: none;
    background: red;
}

#descriptionInput {
     width: 100%; 
    //padding: 10px;
    height: 17px;
    background-color: #ccc;
    border: none;
    outline: none;
    //border-radius: 5px; 
}

#sendButton {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 10px;
    border-radius: 5px;
    cursor: pointer;
}

#sendButton:hover {
    background-color: #0056b3;
}

#chat-container {
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
    width: 900px;
    background-color: white;
    margin-top: 30px;
    // background: #ccc;
}

#header {
    margin-bottom: 20px;
    text-align: center;

}

#chat-history {
    z-index: 9;
    max-height: 400px;
    overflow-y: auto;
    margin-bottom: 10px;
    
}

.message {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    margin-bottom: 10px;
}

.message img {
    max-width: 100%;
    max-height: 200px;
    border-radius: 8px;
}

.message .text {
    margin-top: 5px;
    max-width: 100%;
    word-wrap: break-word;
}

.user-message {
    align-items: flex-end;
}

.user-message .text {
    background-color: #d1e7dd;
    color: #0f5132;
    padding: 10px;
    border-radius: 10px;
}

.bot-message .text {
    background-color: #cce5ff;
    color: #004085;
    padding: 10px;
    border-radius: 10px;
}

form, #image-chat-form {
    display: flex;
    align-items: center;
    gap: 10px;
}

input[type="file"] {
    flex-grow: 1;
   background-color: #ccc;
   // border-radius: 5px;
    padding: 5px;
}

input[type="text"] {
    flex-grow: 2;
    padding: 10px;
   background-color: #ccc;
    //border-radius: 5px;
}

/* Styles for the modal */
.modal {
    display: none; /* Hidden by default */
    position: fixed; /* Stay in place */
    z-index: 9999; /* Sit on top */
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto; /* Enable scroll if needed */
    background-color: rgba(0, 0, 0, 0.8); /* Black background with opacity */
    justify-content: center;
    align-items: center;
    text-align: center;
}

/* Container for modal content and close button */
.modal-content-container {
    position: relative;
    max-width: 90%;
    max-height: 80%;
    margin: auto;
}

.modal-content {
    max-width: 100%;
    max-height: 100%;
}

.close {
    position: absolute;
    top: 10px;
    right: 10px;
    color: #fff;
    font-size: 40px;
    font-weight: bold;
    cursor: pointer;
}

.close:hover,
.close:focus {
    color: black;
    text-decoration: none;
    cursor: pointer;
}

/* Caption for the image */
#caption {
    color: #ccc;
    padding: 10px 20px;
    text-align: center;
}


